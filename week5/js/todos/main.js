import Todos from './todos.js';

const todo = new Todos();




// todo.test();
//would be good to have an onload function that either enters a starter message into the container or grabs the list from LS if available

// document.querySelector('.addTaskBtn').addEventListener('touchend', todo.addTodo);
// document.querySelector('.addTaskBtn').addEventListener('click', todo.addTodo);